/**
 * Shared constant for the external Layout UI product site.
 *
 * Layout UI (https://ui.layout.design once out of staging) is the
 * reskinnable component system: 54 Base UI components on a shared token
 * contract, with every gallery kit installable as a theme.
 */
export const LAYOUT_UI_URL =
  process.env.NEXT_PUBLIC_LAYOUT_UI_URL || "https://ui.staging.layout.design";
